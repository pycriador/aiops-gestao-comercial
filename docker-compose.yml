# Local development only. Production runs on Cloudflare Workers + Lovable Cloud.
services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
